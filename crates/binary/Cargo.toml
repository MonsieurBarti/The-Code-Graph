[package]
name = "binary"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "code-graph"
path = "src/main.rs"

[dependencies]
cli = { path = "../cli" }
domain = { path = "../domain" }
clap = { version = "4", features = ["derive"] }
