[package]
name = "domain"
version = "0.1.0"
edition = "2021"

[dependencies]
globset = "0.4"
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
thiserror = "2"
tracing = "0.1"
rand = "0.9"

[dev-dependencies]
serde_json = "1"
