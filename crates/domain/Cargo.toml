[package]
name = "the-code-graph-domain"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Core domain types and traits for The Code Graph"

[dependencies]
globset = "0.4"
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
thiserror = "2"
tracing = "0.1"
rand = "0.9"

[dev-dependencies]
serde_json = "1"
