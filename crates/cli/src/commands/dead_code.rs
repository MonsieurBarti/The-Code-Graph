use domain::error::Result;

use crate::commands::DeadCodeArgs;
use crate::output::OutputFormat;

pub fn run_dead_code(_args: &DeadCodeArgs, _output_format: OutputFormat) -> Result<()> {
    todo!("dead code command not yet implemented")
}
